using System;
using System.Linq;
using System.Collections.Generic;
using System.Threading.Tasks;
using Microsoft.EntityFrameworkCore;
using JHipsterNet.Core.Pagination;
using JHipsterNet.Core.Pagination.Extensions;
using <%= namespace %>.Domain.Entities;
using <%= namespace %>.Domain.Repositories.Interfaces;
using <%= namespace %>.Infrastructure.Data.Extensions;

namespace <%= namespace %>.Infrastructure.Data.Repositories
{
    public class PositionRepository : GenericRepository<Position, int>, IPositionRepository
    {
        public PositionRepository(IUnitOfWork context) : base(context)
        {
        }

        public override async Task<Position> CreateOrUpdateAsync(Position posistion)
        {
            List<Type> entitiesToBeUpdated = new List<Type>();
            return await base.CreateOrUpdateAsync(posistion, entitiesToBeUpdated);
        }
    }
}
