// Licensed to the .NET Foundation under one or more agreements.
// The .NET Foundation licenses this

using <%= namespace %>.Application.Queries;
using <%= namespace %>.Dto;
using MediatR;
using System.Collections.Generic;

namespace <%= namespace %>.Helpers.SearchEntities
{
    public class SearchdQueryRequest<TEntity> : IRequest<PaginatedResult<TEntity>> where TEntity : class
    {
        public List<SearchQuery> Filters { get; set; }
        public int PageNumber { get; set; }
        public int PageSize { get; set; }
        public string Sort { get; set; }
    }
}
